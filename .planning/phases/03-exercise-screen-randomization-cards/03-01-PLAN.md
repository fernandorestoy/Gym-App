---
phase: 03-exercise-screen-randomization-cards
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/exercises.js
  - js/svgIllustrations.js
  - test/app.test.js
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - ROUT-05
  - CARD-03

must_haves:
  truths:
    - "Every exercise in exercises.js has an svgKey that matches a real pose in svgIllustrations.js (no exercise renders the fallback SVG)"
    - "SVG label text reads START and END in English, not Spanish"
    - "exercises.js contains exactly 18 exercises per muscle group (126 total), enabling the test suite to pass"
    - "All exercise IDs follow the kebab-case pattern and are unique across the library"
  artifacts:
    - path: "js/exercises.js"
      provides: "Full exercise library — 7 groups × 18 exercises = 126 entries, all with correct Spanish kebab-case svgKey values"
      min_lines: 900
    - path: "js/svgIllustrations.js"
      provides: "English label strings: START, END, Exercise illustration"
    - path: "test/app.test.js"
      provides: "Updated test assertions that match the English strings now in svgIllustrations.js"
  key_links:
    - from: "js/exercises.js svgKey field"
      to: "js/svgIllustrations.js poses object keys"
      via: "getSvg(exercise.svgKey, '#82CDD8') call in renderCard()"
      pattern: "svgKey.*:.*\"[a-z].*-[a-z]\""
---

<objective>
Establish the data foundation that Wave 2 rendering depends on: correct svgKey values, English SVG labels, and a complete 18-exercise-per-group library.

Purpose: renderCard() in 03-02 calls getSvg(exercise.svgKey, '#82CDD8'). If any svgKey is camelCase (the current state), getSvg hits the fallback and shows a generic silhouette. All 21 existing exercises must have their svgKeys updated to Spanish kebab-case. The library must then expand to 18 per group to satisfy the test contract at line 20 of app.test.js. svgIllustrations.js must use English strings before the tests for those strings can pass.

Output: js/exercises.js with 126 exercises and correct svgKeys, js/svgIllustrations.js with English labels, test/app.test.js with updated assertions.
</objective>

<execution_context>
@/Users/Fer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Fer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-exercise-screen-randomization-cards/03-RESEARCH.md
@js/svgIllustrations.js
@js/exercises.js
@test/app.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix svgKey values in the 21 existing exercises and update svgIllustrations.js English strings</name>
  <files>js/exercises.js, js/svgIllustrations.js, test/app.test.js</files>
  <action>
**Part A — js/exercises.js: replace all 21 svgKey values**

Apply these exact replacements (field: old value → new value):

| exercise id   | old svgKey (broken)      | new svgKey (correct)             |
|---------------|--------------------------|----------------------------------|
| chest-001     | benchPress               | press-pecho-plano                |
| chest-002     | inclineBenchPress        | press-inclinado-mancuernas       |
| chest-003     | dumbbellFly              | aperturas-mancuernas             |
| back-001      | bentOverRow              | remo-inclinado-bilateral         |
| back-002      | pullUp                   | jalon-pecho                      |
| back-003      | latPulldown              | jalon-pecho                      |
| biceps-001    | barbellCurl              | curl-biceps                      |
| biceps-002    | hammerCurl               | curl-martillo                    |
| biceps-003    | inclineDumbbellCurl      | curl-concentrado                 |
| triceps-001   | ropePushdown             | pushdown-triceps                 |
| triceps-002   | overheadExtension        | extension-triceps-overhead       |
| triceps-003   | closeGripBenchPress      | press-frances-maquina            |
| shoulders-001 | dumbbellOverheadPress    | press-militar-mancuernas         |
| shoulders-002 | lateralRaise             | elevacion-lateral                |
| shoulders-003 | facePull                 | face-pull                        |
| abs-001       | plank                    | plancha-banda                    |
| abs-002       | cableCrunch              | crunch-cable                     |
| abs-003       | hangingLegRaise          | elevacion-piernas-romana         |
| legs-001      | backSquat                | sentadilla-goblet                |
| legs-002      | romanianDeadlift         | peso-muerto-rumano-mancuernas    |
| legs-003      | legPress                 | prensa-piernas                   |

Also update the JSDoc comment on line 17 of exercises.js:
- Change `svgKey` description from `camelCase. e.g. "benchPress"` to `Spanish kebab-case. e.g. "press-pecho-plano"` to reflect the corrected convention.

**Part B — js/svgIllustrations.js: 5 string replacements**

Make these exact string replacements (do NOT change any surrounding code):

1. Line 1067: `"INICIO"` → `"START"`
2. Line 1069: `"FIN"` → `"END"`
3. Line 1082: `"Ejercicio"` → `"Exercise"`
4. Line 1051: `aria-label="Ilustración del ejercicio"` → `aria-label="Exercise illustration"`
5. Line 1079: `aria-label="Ilustración del ejercicio"` → `aria-label="Exercise illustration"`

**Part C — test/app.test.js: update the two test assertions that reference Spanish strings**

The test "every exercise renders an inline SVG illustration and unknown ids fall back" currently asserts Spanish strings that will no longer exist after Part B. Update:

Line 42: `assert.match(svg, /Ilustración del ejercicio/);`
→ `assert.match(svg, /Exercise illustration/);`

Line 47: `assert.match(fallback, /Ejercicio/);`
→ `assert.match(fallback, /Exercise/);`

No other changes to test/app.test.js in this task — the library count assertions (line 20: `allExercises.length === 108`, line 23: each group has 18) will be satisfied in Task 2.
  </action>
  <verify>
    <automated>cd "/Users/Fer/Desktop/Claude's Workspace/Projects/Personal/Gym App (Antigravity)" && node --test test/app.test.js 2>&1 | grep -E "exercise SVG internals|every exercise renders|FAIL|PASS|Error"</automated>
  </verify>
  <done>
- "exercise SVG internals are namespaced by exercise id" test passes (it already calls getSvg with real pose keys like "sentadilla-goblet" and "curl-biceps")
- "every exercise renders an inline SVG illustration and unknown ids fall back" test no longer fails on the Spanish string assertions (it may still fail on the library count — that is expected until Task 2)
- svgIllustrations.js contains no occurrences of "INICIO", "FIN", "Ilustración del ejercicio", or "Ejercicio"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand exercises.js to 18 exercises per muscle group (126 total)</name>
  <files>js/exercises.js</files>
  <behavior>
    - Test: allExercises.length === 108 (line 20 of app.test.js) — passes only when 7 groups × 18 = 126... wait: the test asserts 108. Re-read: line 20 says `assert.equal(allExercises.length, 108)`. But 7 × 18 = 126, not 108. This is a pre-existing discrepancy in the test file. The test at line 23 says each group has 18 exercises (7 × 18 = 126), but line 20 says 108. Line 20 is wrong — it was written when there were 6 groups. Honor line 23 (each group has 18) as the authoritative constraint. The executor must update line 20 to `assert.equal(allExercises.length, 126)` in test/app.test.js as part of this task.
    - Test: each muscle group array has exactly 18 entries
    - Test: all exercise IDs are unique (byId.size === allExercises.length)
    - Test: every exercise has id (string, kebab-case), name (string), description (string), videoId (string), svgKey (string)
  </behavior>
  <action>
**Step 0 — Fix the test count assertion**

In test/app.test.js, update line 20:
`assert.equal(allExercises.length, 108);`
→ `assert.equal(allExercises.length, 126);`

**Step 1 — Add 15 exercises to each group** (each group currently has 3; target is 18)

Use ONLY svgKey values that exist in svgIllustrations.js. The complete verified pose inventory per group is in 03-RESEARCH.md Section 1. Use those exact Spanish kebab-case keys.

ID convention: `{group}-{NNN}` zero-padded 3 digits, continuing from the existing max per group (currently all groups end at 003, so new IDs start at 004).

**Required additions per group** — use these svgKey values (all verified present in svgIllustrations.js):

**chest** (add chest-004 through chest-018, 15 exercises):
Use these svgKeys in order: press-declinado-mancuernas, pullover-mancuerna, squeeze-press, press-pecho-maquina, pec-deck, press-inclinado-maquina, crossover-maquina, smith-press-pecho, press-declinado-maquina, crossover-cables, press-bandas-pecho, aperturas-cables-bajo, aperturas-cables-alto, press-inclinado-cable, cruce-cable-unilateral

**back** (add back-004 through back-018, 15 exercises):
Use these svgKeys in order: remo-mancuerna, peso-muerto-mancuernas, pullover-espalda, remo-kroc, encogimientos-mancuernas, remo-sentado-maquina, jalon-agarre-cerrado, remo-tbar, pulldown-brazos-rectos, hiperextension, remo-cable, face-pull, jalon-banda, remo-bajo-cable, pullover-cable

**biceps** (add biceps-004 through biceps-018, 15 exercises):

The Arms SVG pool has 18 keys. biceps-001/002/003 already use: `curl-biceps`, `curl-martillo`, `curl-concentrado`. Use the remaining 15 Arms keys for biceps-004 through biceps-018, in this order:

`curl-zottman`, `curl-maquina-biceps`, `extension-triceps-overhead`, `curl-predicador-maquina`, `kickback-triceps`, `curl-scott-maquina`, `extension-triceps-maquina`, `press-frances-maquina`, `curl-cable`, `pushdown-triceps`, `curl-banda`, `extension-banda-overhead`, `curl-inverso-polea`, `extension-triceps-banda`, `dip-asistido`

**triceps** (add triceps-004 through triceps-018, 15 exercises):

triceps-001/002/003 already use: `pushdown-triceps`, `extension-triceps-overhead`, `press-frances-maquina`. Use the remaining 15 Arms keys for triceps-004 through triceps-018, in this order:

`curl-biceps`, `curl-martillo`, `curl-concentrado`, `curl-zottman`, `curl-maquina-biceps`, `curl-predicador-maquina`, `kickback-triceps`, `curl-scott-maquina`, `extension-triceps-maquina`, `curl-cable`, `curl-banda`, `extension-banda-overhead`, `curl-inverso-polea`, `extension-triceps-banda`, `dip-asistido`

**Complete verified pose key lists for all other groups (from 03-RESEARCH.md):**
- Legs/Glutes (18 keys): sentadilla-goblet, zancada-caminando, peso-muerto-rumano-mancuernas, sentadilla-sumo, step-up, sentadilla-bulgara, prensa-piernas, extension-cuadriceps, curl-isquiotibiales, hack-squat, aduccion-cadera, abduccion-cadera, patada-gluteo-polea, sentadilla-banda, peso-muerto-banda, clamshell-banda, puente-gluteos-banda, monster-walk
- Chest (18 keys): press-pecho-plano, press-inclinado-mancuernas, aperturas-mancuernas, press-declinado-mancuernas, pullover-mancuerna, squeeze-press, press-pecho-maquina, pec-deck, press-inclinado-maquina, crossover-maquina, smith-press-pecho, press-declinado-maquina, crossover-cables, press-bandas-pecho, aperturas-cables-bajo, aperturas-cables-alto, press-inclinado-cable, cruce-cable-unilateral
- Back (18 keys): remo-mancuerna, remo-inclinado-bilateral, peso-muerto-mancuernas, pullover-espalda, remo-kroc, encogimientos-mancuernas, jalon-pecho, remo-sentado-maquina, jalon-agarre-cerrado, remo-tbar, pulldown-brazos-rectos, hiperextension, remo-cable, face-pull, jalon-banda, remo-bajo-cable, pullover-cable, remo-alto-cable
- Shoulders (18 keys): press-militar-mancuernas, elevacion-lateral, elevacion-frontal, pajaros-reversefly, press-arnold, encogimientos-hombros, press-hombro-maquina, elevacion-lateral-maquina, reverse-pec-deck, smith-press-hombro, maquina-encogimientos, press-sentado-maquina-hombro, elevacion-lateral-cable, face-pull-cable, elevacion-frontal-banda, press-banda-hombros, rotacion-externa-banda, pull-apart-banda
- Abs/Core (18 keys): crunch-mancuerna, russian-twist, plancha-remo, woodchop-mancuerna, farmers-carry, side-bend, crunch-maquina, rotacion-torso-maquina, elevacion-piernas-romana, ab-roller-maquina, hiperextension-inversa, crunch-polea-alta, pallof-press, woodchop-cable, crunch-cable, anti-rotacion-banda, dead-bug-banda, plancha-banda

Each group above has exactly 18 keys. Map them 1:1 to exercises — first 3 keys are already used by existing exercises, add the remaining 15 as new entries.

**Exercise content for new entries:** Use realistic exercise names and descriptions for each muscle group. Write concise 2-3 sentence descriptions in the same imperative style as the existing entries. Assign plausible YouTube video IDs (use the same IDs already in the file for Phase 3 — Phase 4 will verify and replace all video IDs with real ones).

**Step 2 — Verify format consistency**

Every new exercise object must have all 6 fields: id, name, group, description, videoId, svgKey. No missing fields. The id must be unique and match the kebab-case pattern `^[a-z0-9-]+$`.
  </action>
  <verify>
    <automated>cd "/Users/Fer/Desktop/Claude's Workspace/Projects/Personal/Gym App (Antigravity)" && node --test test/app.test.js 2>&1</automated>
  </verify>
  <done>
- `node --test test/app.test.js` exits 0 (all tests pass)
- "exercise database has complete, unique exercise definitions" passes: allExercises.length === 126, each group has 18 exercises, all IDs unique, all fields present
- "exercise SVG internals are namespaced by exercise id" passes
- "every exercise renders an inline SVG illustration and unknown ids fall back" passes with English strings
- "workout days build the expected muscle-group mix" passes (pickRandom still works with larger pools)
- "pickRandom uses a bounded Fisher-Yates shuffle and never mutates input" passes (unchanged)
- "escapeHtml encodes dynamic text before template rendering" passes (unchanged)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exercises.js → getSvg() | svgKey values are hardcoded in exercises.js (not user input) — controlled data |
| renderCard() → innerHTML | SVG string from getSvg() is injected via template literal — safe because accentColor is hardcoded '#82CDD8', not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | exercises.js svgKey values | accept | All svgKey values are hardcoded constants; no user input touches this path |
| T-03-01-02 | Information Disclosure | YouTube videoId in exercises.js | accept | videoIds are public YouTube IDs; no PII, no secrets |
</threat_model>

<verification>
Run after both tasks complete:

```
cd "/Users/Fer/Desktop/Claude's Workspace/Projects/Personal/Gym App (Antigravity)" && node --test test/app.test.js
```

All 6 tests must pass. Zero failures.

Additionally verify manually in svgIllustrations.js:
```
grep -n "INICIO\|FIN\|Ilustración\|Ejercicio" "/Users/Fer/Desktop/Claude's Workspace/Projects/Personal/Gym App (Antigravity)/js/svgIllustrations.js"
```
Must return no output (all Spanish strings replaced).
</verification>

<success_criteria>
- All 6 tests in test/app.test.js pass with `node --test`
- exercises.js has 126 exercises: 7 groups × 18 each
- All 21 original svgKey values are Spanish kebab-case and match real pose keys in svgIllustrations.js
- svgIllustrations.js contains "START", "END", "Exercise illustration" — no Spanish strings remain
- test/app.test.js line 20 asserts 126 (not 108), line 42 asserts /Exercise illustration/, line 47 asserts /Exercise/
</success_criteria>

<output>
After completion, create `.planning/phases/03-exercise-screen-randomization-cards/03-01-SUMMARY.md` following the summary template at `@/Users/Fer/.claude/get-shit-done/templates/summary.md`.
</output>
